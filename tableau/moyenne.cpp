#include <iostream>
#include <vector>

int main(){
    std::cout << "entrer des valeurs : " << std::endl;
    std::vector<int> valeurs {};
    int ajout {0};
    while(ajout>=0){
        while(std::cin>>ajout)

    }
}